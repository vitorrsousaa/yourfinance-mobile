const DATABASE_NAME = '@yourFinance';
/**
 * @app:collection
 */

const USER_COLLECTION = `${DATABASE_NAME}:user`;

const TOKEN_COLLECTION = `${DATABASE_NAME}:token`;

export { TOKEN_COLLECTION,USER_COLLECTION };
